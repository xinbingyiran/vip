
const init = (ts, mainBoard, infoBoard) => {
}

const status = {
    score: 0,
    level: 0,
    over: false,
};
const update = (ts, addKeys, removeKeys) => { }

const options = {}

export default { status, init, update, options }